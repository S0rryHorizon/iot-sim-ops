# FastAPI mock for iot-sim-ops
# Copy this file to `.env` and adjust values as needed.

# MySQL connection
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=
DB_NAME=iot_sim_ops

# CORS
# Comma separated origins, use * for any
CORS_ORIGINS=*
CORS_ALLOW_CREDENTIALS=false

